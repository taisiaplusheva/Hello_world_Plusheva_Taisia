#!/bin/bash

echo "Файловая система | Заполнение"
df -h | awk 'NR>1 {
    printf "%-16s | %s\n", $1, $5
    gsub(/%/, "", $5)
    if ($5 > 90) {
        print "ВНИМАНИЕ: " $1 " заполнен на " $5 "%!"
    }
}'
